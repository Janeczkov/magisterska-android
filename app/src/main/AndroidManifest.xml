<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apkafinal">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
    <activity android:name=".klasy.MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

        <activity android:name=".klasy.logowanie" />
        <activity android:name=".klasy.rejestracja" />
        <activity android:name=".klasy.zalogowany" />
        <activity android:name=".klasy.dodajPlik" />
        <activity android:name=".klasy.materialy" />
        <activity android:name=".klasy.sprawdzanieUzytkownikow" />
        <activity android:name=".klasy.statystykiUzytkownika" />
        <activity android:name=".klasy.admin" />
        <activity android:name=".klasy.statystykiPliku" />
        <activity android:name=".klasy.statystykiSerwera" />
    </application>
</manifest>
